"""Per-Return-domain helper modules.

Anything that's specific to one weekly event (cult internals, story
mechanics, future-event domain logic) belongs in here in its own module,
so :mod:`cogs.events.returns._common` stays focused on truly generic
helpers (tier resolution, ``~manage_return`` feedback delivery, etc.).
"""
